<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="party.msdg.renova.mock.GenTaskDao">
    <select id="one" parameterType="int" resultType="GenTask">
        select id, name, tableId, num, status, remark
        from t_gen_task where id = #{id}
    </select>
</mapper>