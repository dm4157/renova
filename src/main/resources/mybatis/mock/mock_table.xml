<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="party.msdg.renova.mock.database.MockTableDao">
    <select id="one" parameterType="int" resultType="MockTable">
        select *
        from t_db_mock_table where id = #{id}
    </select>
</mapper>